using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Spine;
using Spine.Unity;

public class AnimatePlz : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log ("AnimatePlz " + name);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
